# System-Monitor-Tool
This project implements a command-line system monitoring utility, analogous to the Unix top command, built entirely in C++. The tool provides real-time visibility into system resource utilization and active processes on a Linux operating system.
